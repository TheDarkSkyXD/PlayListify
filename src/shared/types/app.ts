// App information interface
export interface AppInfo {
  name: string;
  version: string;
  electronVersion: string;
  nodeVersion: string;
  chromiumVersion: string;
  platform: string;
  arch: string;
  userDataPath: string;
} 