/**
 * This file contains a function to test logging from the renderer process
 */

export function testRendererLogging() {
  // Function kept for compatibility, but test logs removed
  return 'Logging test completed';
}
