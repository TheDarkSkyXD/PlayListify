import { SQLiteAdapter } from '../../src/sqlite_adapter';

describe('SQLiteAdapter Edge Cases', () => {
    it('EC.1: Should have at least one test', () => {
        expect(true).toBe(true);
    });
});